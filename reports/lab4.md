# Lab 4 报告

## 实验目的

实现基于 SV39 的虚拟内存与用户地址空间隔离。

## 实现内容

- 页表与地址映射
- 用户地址空间 `MemorySet`
- `mmap` / `munmap` / `sys_trace` 等（如适用）

## 关键设计

（在此补充页表项权限、trap 上下文映射与缺页处理等说明。）

## 测试结果

本地 / CI 测例通过情况：

## 问题与总结

（在此记录调试过程与收获。）
